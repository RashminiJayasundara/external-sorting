#include <iostream>
#include <vector>
#include <map>
#include "Leaf.h"
using namespace std;

const uint64_t BUFFER_SIZE = 0.75 * 1024 * 1024;

void Leaf::fillBuffer(const vector<map<string, vector<uint32_t>>> &inputBuffer1, const vector<map<string, vector<map<uint32_t, int>>>> &inputBuffer2, string fileName, vector<map<uint32_t, int>> &buffer)
{
    if (buffer.size() <)
    {
    }
}
Leaf::Leaf(string fileName)
{
    value;
    buffer;
    fileName = fileName;
}
